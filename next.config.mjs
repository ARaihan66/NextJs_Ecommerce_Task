///** @type {import('next').NextConfig} */
//const nextConfig = {};

//export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
    // Custom configuration options
    images: {
      domains: ['fakestoreapi.com'], // Allow loading images from this domain
    },
    // Other Next.js configuration options can be added here
  };
  
  export default nextConfig;
  

